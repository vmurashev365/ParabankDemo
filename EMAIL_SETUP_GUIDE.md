# 📧 Настройка Email Уведомлений - Пошаговая Инструкция

## 🎯 Почему email не приходит?

Система автоматизации создана и работает, но email не отправляется потому что:
1. ❌ Используются **тестовые** credentials в `.env` файле
2. ❌ Нет реального Gmail App Password
3. ❌ Система не запущена в production режиме

## 🔧 Настройка Gmail для отправки email

### Шаг 1: Подготовка Gmail аккаунта

1. **Включить 2FA (двухфакторную аутентификацию)**:
   - Перейти в [Google Account Settings](https://myaccount.google.com/security)
   - Найти "2-Step Verification" 
   - Включить двухфакторную аутентификацию

2. **Создать App Password**:
   - В настройках Google найти "App passwords"
   - Выбрать "Mail" и ваше устройство
   - Скопировать **16-символьный пароль** (например: `abcd efgh ijkl mnop`)

### Шаг 2: Обновление .env файла

Отредактировать файл `.env` с реальными данными:

```env
# Замените эти строки:
EMAIL_USER=your-real-email@gmail.com
EMAIL_PASS=your-16-char-app-password
RECIPIENT_EMAIL=vmurashev@gmail.com

# Пример:
EMAIL_USER=myemail@gmail.com
EMAIL_PASS=abcd efgh ijkl mnop
RECIPIENT_EMAIL=vmurashev@gmail.com
```

### Шаг 3: Тестирование email отправки

```bash
# Тест отправки email
npm run email:test
```

**Ожидаемый результат**:
```
✅ Email sent successfully!
📫 Check inbox: vmurashev@gmail.com
```

## 🚀 Способы запуска автоматизированной отправки

### Вариант 1: Немедленная отправка
```bash
# Запустить тесты и отправить email сейчас
npm run automation:run
```

### Вариант 2: Тестовая отправка (через 5 секунд)
```bash
# Для проверки системы
npm run automation:test
```

### Вариант 3: Автоматический ночной запуск
```bash
# Запустить планировщик (каждый день в 2:00 AM)
npm run automation:start
```

## 📧 Что будет в email уведомлении

Email содержит:
- **📊 Executive Summary**: Общая статистика тестов
- **📈 Подробная статистика**: Количество passed/failed/skipped тестов
- **🎯 Анализ производительности**: Success rate и время выполнения
- **🔗 Рабочие ссылки на отчеты**:
  - 📊 Allure Report (интерактивный дашборд)
  - 🎭 Playwright Report (кросс-браузерные результаты)
  - 🥒 Cucumber Report (BDD отчет)

## 🧪 Пример реального email

После настройки credentials email будет выглядеть так:

```
Subject: ✅ ParaBank Nightly Tests - 98.7% Success Rate (150/152)

📊 ParaBank Test Execution Report
=================================

🎯 Executive Summary
- Success Rate: 98.68%
- Total Tests: 152
- Execution Time: 5m 47s
- Environment: Production Nightly

📈 Test Statistics
- ✅ Passed: 150 scenarios
- ❌ Failed: 2 scenarios  
- ⏭️ Skipped: 0 scenarios

[📊 Allure Report] [🎭 Playwright Report] [🥒 Cucumber Report]
```

## 🔍 Диагностика проблем

### Если email не отправляется:

1. **Проверить credentials**:
   ```bash
   npm run email:test
   ```

2. **Проверить .env файл**:
   ```bash
   # Убедиться что нет тестовых значений
   cat .env | grep EMAIL
   ```

3. **Проверить Gmail настройки**:
   - 2FA включена
   - App Password создан правильно
   - Аккаунт не заблокирован

### Частые ошибки:

❌ **"Invalid login"** → Неправильный App Password  
❌ **"ENOTFOUND"** → Проблемы с интернетом  
❌ **"Authentication failed"** → 2FA не включена  

## ✅ Готовность к production

После правильной настройки:

1. **Тест системы**: `npm run email:test` → ✅ Success
2. **Ручной запуск**: `npm run automation:run` → Email отправлен
3. **Автоматический режим**: `npm run automation:start` → Ежедневные отчеты

## 📞 Поддержка

Если возникают проблемы:
- Проверить настройки по этой инструкции
- Протестировать `npm run email:test`
- Убедиться что `.env` содержит реальные credentials

---

**🎯 Результат**: После выполнения этих шагов email с результатами тестов будет автоматически приходить на `vmurashev@gmail.com` каждую ночь в 2:00 AM по московскому времени! 📧✨
