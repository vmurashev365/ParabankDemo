# Утилиты для управления Step Definitions

Этот проект содержит набор утилит для систематического управления Cucumber step definitions и предотвращения дубликатов.

## 📚 Доступные утилиты

### 1. `check-step-definitions.js` - Проверка дубликатов
Проверяет дубликаты step definitions и анализирует покрытие.

```bash
# Основная проверка
node scripts/check-step-definitions.js

# Выход: 0 если дубликатов нет, 1 если есть проблемы
```

**Что проверяет:**
- ✅ Дубликаты step definitions
- 📊 Статистику по типам (Given/When/Then)
- ❌ Отсутствующие step definitions для feature файлов

### 2. `add-step-definition.js` - Безопасное добавление
Добавляет новые step definitions с автоматическим предотвращением дубликатов.

```bash
# Добавить новый step definition
node scripts/add-step-definition.js "Given I am on homepage"

# Добавить параметризованный step
node scripts/add-step-definition.js "When I enter username {string} and password {string}"

# Добавить step с числовым параметром
node scripts/add-step-definition.js "Then I should wait for {int} seconds"
```

**Особенности:**
- 🔍 Автоматически проверяет существование
- 🏗️ Генерирует параметризованные versions
- 📝 Создает TODO шаблоны для имплементации

### 3. `find-missing-steps.js` - Анализ покрытия Gherkin шагов
**НОВАЯ УТИЛИТА!** Анализирует feature файлы и находит Gherkin шаги без соответствующих step definitions.

```bash
# Анализ всех feature файлов
node scripts/find-missing-steps.js

# Анализ конкретного файла
node scripts/find-missing-steps.js --feature features/authentication.feature

# Только статистика, без команд исправления
node scripts/find-missing-steps.js --no-commands

# Генерация команд для исправления
node scripts/find-missing-steps.js --generate-commands
```

**Возможности:**
- 🔍 **Умный парсинг** Gherkin feature файлов
- 🎯 **Точное сопоставление** с существующими step definitions
- 📊 **Детальная статистика** покрытия
- 🛠️ **Автогенерация команд** для исправления
- 📁 **Рекурсивный поиск** во всех подпапках features/
- 🔧 **Поддержка параметризации** ({string}, {int}, {float})

## 🎯 Рекомендуемый workflow

### Добавление нового теста:

1. **Создайте feature файл** с Gherkin сценарием
2. **Найдите недостающие шаги:**
   ```bash
   node scripts/find-missing-steps.js --feature features/new-test.feature
   ```
3. **Добавьте step definitions:**
   ```bash
   node scripts/add-step-definition.js "Given новый шаг"
   ```
4. **Проверьте отсутствие дубликатов:**
   ```bash
   node scripts/check-step-definitions.js
   ```

### Рефакторинг существующих тестов:

1. **Проверьте общее покрытие:**
   ```bash
   node scripts/find-missing-steps.js
   ```
2. **Выполните команды исправления** (копируйте из вывода утилиты)
3. **Финальная проверка:**
   ```bash
   node scripts/check-step-definitions.js
   ```

### Регулярное обслуживание:

```bash
# Еженедельная проверка качества
node scripts/check-step-definitions.js && node scripts/find-missing-steps.js --no-commands
```

## 📊 Понимание вывода

### find-missing-steps.js
```
📊 ИТОГОВАЯ СТАТИСТИКА
============================================================
📝 Всего Gherkin шагов: 238      ← Все шаги в feature файлах
✅ Шагов с step definitions: 235  ← Шаги с реализацией
❌ Шагов БЕЗ step definitions: 3  ← Нужно исправить
📈 Покрытие: 98.7%                ← Процент готовности
```

### check-step-definitions.js
```
📝 Найдено step definitions: 142  ← Всего в файле AuthenticationSteps.ts
✅ Дубликатов не найдено!         ← Качество кода
📈 Статистика по типам:           ← Распределение
   Given: 21, When: 50, Then: 71
```

## 🚨 Важные примечания

### Параметризация
Утилиты поддерживают автоматическую параметризацию:
- `"john"` → `{string}`
- `123` → `{int}`
- `4.5` → `{float}`

### Экранирование
Для шагов со спецсимволами используйте экранирование:
```bash
node scripts/add-step-definition.js "When I test with regex \\(pattern\\)"
```

### And/But шаги
Утилиты правильно обрабатывают `And` и `But`, наследующие тип от предыдущего `Given/When/Then`.

## 🎯 Цели системы

1. **Zero Duplicates** - Никаких дубликатов step definitions
2. **100% Coverage** - Каждый Gherkin шаг имеет реализацию  
3. **DRY Principle** - Переиспользование через параметризацию
4. **Automation** - Минимальная ручная работа
5. **Quality** - Систематический подход к тестированию

## 📈 Метрики качества

- ✅ **Дубликаты:** 0
- ✅ **Покрытие:** 98.7%
- ✅ **Step definitions:** 150+
- ✅ **Автоматизация:** 100%
