# ParaBank Automated Test System Configuration
# Copy this file to .env and update with your actual credentials

# Email Configuration for Gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Test Results Recipient
RECIPIENT_EMAIL=vmurashev@gmail.com

# Optional: Custom Cron Schedule (default: 0 2 * * * = daily at 2 AM)
# CRON_SCHEDULE=0 2 * * *

# Optional: Environment Identifier
NODE_ENV=production

# Optional: Additional Email Recipients (comma-separated)
# CC_EMAILS=manager@company.com,qa-team@company.com

# Optional: Email From Name
EMAIL_FROM_NAME=ParaBank Test System

# Security: Encryption Key for Sensitive Data (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# ENCRYPTION_KEY=your-32-char-hex-key-here
